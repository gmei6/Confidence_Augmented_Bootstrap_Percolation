# macOS
.DS_Store

#Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
dist/

# C++ / CMake
build/
*.o
*.so
*.a

# Results — raw per-realization outcomes are large and regenerable
# from (config, seed, commit). Keep the dir, ignore its contents.
results/raw/*
results/figures/*
!results/raw/.gitkeep
!results/figures/.gitkeep

# Editor — optional; keep if you want shared settings, remove if not
# .vscode/